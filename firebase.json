{
    "firestore": {
        "rules": "firestore.rules"
    },
    "storage": [
        {
            "rules": "storage.rules",
            "bucket": "sprachnotizen-app.firebasestorage.app"
        }
    ]
}